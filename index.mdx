import { Head } from "mdx-deck";
export { future as theme } from "mdx-deck/themes";

<Head>
    <title>Knowledge Session (2) 🤯</title>
</Head>

# Knowledge Session (2) 🤯

---

# What's New

import { Appear } from "mdx-deck";

<Appear>
    <h2>Private Class Fields</h2>
    <h2>V8 v7.4</h2>
    <h2>React Router v5</h2>
</Appear>

---

## Private Class Fields

-   [tc39/proposal-class-fields](https://github.com/tc39/proposal-class-fields#private-fields)
-   [tc39 process](https://tc39.github.io/process-document/)
-   [hold on...tc39??](https://github.com/tc39)

---

import { CodeSurfer } from "mdx-deck-code-surfer";

<CodeSurfer
    title="Private Class Fields"
    code={require("!raw-loader!./code/private.js")}
    lang="javascript"
    showNumbers={true}
    dark={false}
    steps={[
        { notes: "private class and methods in JS?" },
        { lines: [2], notes: "Private Field" },
        { range: [9, 11], notes: "Private Method" },
        { range: [15, 16], notes: "Invalid invocations" }
    ]}
/>

---

## V8 release v7.4

-   WebAssembly threads🙈
-   Private class fields
-   Locale
-   Wait... [V8](https://v8.dev/) ?

---

## React Router v5

-   was v4.4 but... lol, here is [why](https://reacttraining.com/blog/react-router-v5/)
-   `<Route path>` accept array
-   backwards compatibility

---

## MyReads

-   Bookmark [Rubric](https://review.udacity.com/#!/rubrics/918/view)
-   Review steps in coding stage (Concept #6)
-   Demo? 🙊

---

## [Lifting state up](https://reactjs.org/docs/lifting-state-up.html)

---

## set State

-   [setState](https://reactjs.org/docs/react-component.html#setstate)
-   batching
-   shallow update
-   with prev state/props

---

<CodeSurfer
    title="setState"
    code={require("!raw-loader!./code/setstate.js")}
    lang="javascript"
    showNumbers={true}
    dark={false}
    steps={[
        { notes: "setState closer look" },
        { range: [3, 5], notes: "batching, only one render instead of 3" },
        {
            lines: [9],
            notes: "this will overwrite state.c due to shallow merge"
        },
        { lines: [11], notes: "instead, we can do this to apply patches" }
    ]}
/>

---

import Code from "mdx-code";

## ES6

-   spread/rest
-   destructuring
-   module import/export

---

export default Code;

```javascript spread
// clone
const a = [1, 2, 3];
const clone = [...a];
// aClone.push(4);
// console.log(a);
// console.log(clone);

const b = { test: 1 };

// concat
const prev = [1, 2, 3];
const next = [4, 5, 6];
const complete = [...prev, ...next];

// spread params
const func = (one, two, three) => {
    console.log(`inputs are ${one}, ${two} and ${three}`);
};
func(...a);
```

---

export default Code;

```javascript rest
// args to array
const a = [1, 2, 3];

const func_prev = (one, two, three) => {
    console.log(`inputs are ${one}, ${two} and ${three}`);
};

const func = (...args) => {
    console.log(args);
};

func_prev(...a);
func(...a);
```

---

## Module

### import

### and export

-   [import](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/import#Syntax)
-   [export](https://developer.mozilla.org/en-US/docs/web/javascript/reference/statements/export#Syntax)

---

## Jest

---

## Until next time 👋
